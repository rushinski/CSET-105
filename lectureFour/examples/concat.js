const prompt = require("prompt-sync");

let x = [1,2,3];
let y =[4,5,6];
// concat joins arrays together
console.log(x.concat(y)); // "x" is the array that is being added on to "y" is the array that is being added onto x