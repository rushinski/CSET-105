const arr = [ 1, 2, 3 ];
//Prints each element in the array out on a new line
arr.forEach(element => { 
  console.log(element);
})

/* "arr" is the name of the array ".forEach" calls the forEach function in the "()" you create a function named "element" with a pointer "=>", the element is
then printed with a "console.log" */